.product

  %div
    = image_tag product.image.url(:thumb), class: "width_style"

  .name
    = link_to product.name, showroom_product_path(product)

  .price
    = number_to_currency product.price

.both_style
